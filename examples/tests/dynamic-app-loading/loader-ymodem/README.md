AppLoader Helper
=================

This is a helper application that works in tandem with
the `process_manager` application and enables users to 
load new apps during runtime.

lsb -vv build/cortex-m4/cortex-m4.tbf < /dev/ttys016 > /dev/ttys016

