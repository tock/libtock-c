PWM LED Pulse Test App
======================

This app checks how many PWM pins the board provides, then pulses every pin
up and down in brightness, offsetting each pin's phase so they pulse
slightly out of sync with one another.

These do not _need_ to be LEDs, but that is kind of the intended design for
the pulsing effect.
