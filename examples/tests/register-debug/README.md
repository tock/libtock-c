Crash with Known Register Values
================================

This app immediately crashes, but sets all registers to a known, incrementing
value. This is useful for inspecting debug print output to ensure all
registers are correctly captured and printed.
